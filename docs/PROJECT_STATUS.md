# Статус проекта CreateOrder

> Последнее обновление: 30.08.2026. Этот файл — краткая точка восстановления работы: он должен позволять продолжить проект без истории чата.

## Детальный runbook P1–P6 для Luna — подготовлен 29.08.2026

**Цель:** дать младшей модели последовательные, ограниченные и проверяемые поручения по следующим улучшениям без самостоятельного расширения юридической или пользовательской области.

**Сделано:** создан `.spec/tasks/createorder-p1-p6-luna-implementation-runbook-2026-08-29.md`. В нём зафиксированы 28 задач/гейтов: реальные точки расширения VBA и PowerShell, требования CP1251/CRLF и локализации, запрет записи в P1 preview/P2 scan/P6 simulation, адресные проверки, безопасный импорт в закрытую книгу, документационный чекпойнт и условные owner gate P5/P6.

**Текущий этап:** P0, P1.1–P1.6, P2.1–P2.4, P3.1–P3.4, P4.1–P4.4 и P5.1–P5.4 реализованы, проверены и импортированы/выпущены. В `frmEnrollmentWizardV2` выбранная выплата без основания подсвечивается, а общий экспорт блокируется; невыбранная выплата не валидируется. В P5 единый DOCX собирает сохранённые `ENROLLMENT`/`TRANSFER`/`EXCLUSION` в отдельные `§` по порядку первого появления категории и сохраняет каждую выплату конкретного сотрудника отдельно. После ручной компоновки владельца формы сохраняют designer-геометрию; визуальная проверка DOCX и форм оператором остаётся ручным этапом. P6 не реализуется до утверждения НПА, ролей и матрицы переходов.

**Принятые решения P1.4–P1.5:** отдельная страница `MultiPage`; только существенные строки; совпадение after=before выдаёт предупреждение и не блокирует подтверждение. Добавлены read-only блоки «До», «После», «Выплаты», «Предупреждения» и кнопки «Подтвердить»/«Отмена». Кнопка «Сохранить» сначала открывает preview; запись выполняется только после подтверждения, отмена не меняет реестры, повторное подтверждение безопасно. P2 зафиксировал read-only сканер `mdlPersonnelDataIntegrity`, design-time форму `frmDataIntegrityCenter`, фильтры severity/category и Ribbon-кнопку без автоисправления. Чистая fixture проходит с 0 findings, повреждённая покрывает заявленные категории, а baseline рабочей книги — 8 диагностических находок (6 ошибок, 2 предупреждения). P3.1 зафиксировал режимы Verify/Release, stop rules, коды возврата и матрицу проверок. P5-Q4 реализован так: включённая выплата с пустым `FactualBasis` подсвечивается и блокирует весь экспорт до заполнения или снятия флажка; снятая выплата не проверяется. Статус правила `REQUIRES_DECISION` также блокирует экспорт. Различия выплат между сотрудниками допустимы; процент выше 100% из-за водительской надбавки — предупреждение.

**Проверка:** `tools/Test-GroupedPersonnelOrderSafe.ps1` завершился `GROUPED_PERSONNEL_ORDER_OK` и `GROUPED_PERSONNEL_ORDER_DOCX_OK`: 5 событий в порядке ввода, 3 параграфа, 5 независимых выплат, предупреждение по водительской надбавке, блокировка неполного основания, блокировка `REQUIRES_DECISION` и read-only/multiline форма. Общий Verify после добавления обработчиков изменения основания завершился `exit=0` в `Trash/release-gate-20260830-025544/report.md`, включая обновлённый grouped-тест и полный `ACCEPTANCE_SMOKE_OK`. Финальный `Release -SkipManual` завершился `exit=0` в `Trash/release-gate-20260830-030504/report.md`; создан `CreateOrderReleases/CreateOrder_Release_20260830_031131.xlsm`, SHA-256 `6145731EE72A6456E4670470E4E5BACBBA05693E9A4DEDCD1F852E65D446F29C`; backup: `CreateOrderBackups/release-gate-20260830-030504/CreateOrder.xlsm.before-release-gate.xlsm`. Импорты P5/V2 выполнены в закрытую книгу с резервными копиями `CreateOrderBackups/grouped-personnel-order-installed-20260830-022115`, `CreateOrderBackups/personnel-action-v2-installed-20260830-022855` и `CreateOrderBackups/enrollment-v2-basis-change-handlers-20260830-030409`.

## P2 — центр целостности бизнес-данных завершён 29.08.2026

**Цель:** дать оператору безопасную диагностику кадровых связей до ручного исправления данных.

**Сделано:** добавлены спецификация `.spec/tasks/p2-business-data-integrity.md`, read-only сканер `CreateOrder.xlsm.modules/mdlPersonnelDataIntegrity.bas`, disposable fixture-тест `tools/Test-PersonnelDataIntegritySafe.ps1`, design-time форма `frmDataIntegrityCenter` с 11 статическими контролами, локализация и Ribbon-кнопка. Сканер проверяет идентификаторы, события, снимки, назначения, документы/файлы, текущее состояние, хронологию, активные выплаты, связь со `Штат`, НПА и классификации. Никаких `Select`/`Activate`/записей/автоисправлений в сканировании нет.

**Проверка:** чистая fixture — `findings=0`; повреждённая fixture покрывает `IDENTIFIER`, `EVENT_EMPLOYEE`, `EVENT_SNAPSHOTS`, `SNAPSHOT_LINKAGE`, `ASSIGNMENT_LINKAGE`, `DOCUMENT_LINKAGE`, `CURRENT_STATE_LINKAGE`, `CHRONOLOGY`, `ACTIVE_PAYMENTS`, `STAFF_LINK`, `LEGAL_REFERENCE` и `EXCLUSION_STATE`; schema fixture даёт `SCHEMA=ERROR`; хеши реестров и `Штат` до/после совпадают; P2.4-регрессия зелёная (`ACCEPTANCE_SMOKE_OK`). На реальной книге зафиксирован baseline 8 findings (6 errors, 2 warnings), который требует отдельного операторского разбора и не исправляется автоматически.

**Внедрение:** импорт выполнен после staging в закрытую `CreateOrder.xlsm`; резервная копия: `CreateOrderBackups/data-integrity-center-installed-20260829-220800/CreateOrder.before-data-integrity-center-install.xlsm`. UserGuide дополнен порядком запуска, фильтрации и передачи findings ответственному.

**Следующий конкретный шаг:** P2 завершён; технический release-gate P3, центр истории P4 и групповой приказ P5 завершены. Следующий этап — отдельный owner gate P6: письменные ответы по НПА, ролям, датам действия и переходам правил.

## P3 — единый release-gate завершён 29.08.2026

**Цель:** выпускать `.xlsm` только после воспроизводимой технической проверки и не затрагивать рабочую книгу при ошибке.

**Сделано:** `tools/Invoke-CreateOrderReleaseGate.ps1` реализует Verify/Release, preflight без завершения пользовательских Office-процессов, disposable-копию, сверку источников с VBA-компонентами с учётом CP1251/UTF-8 и служебных `Attribute`, Open XML/Excel read-only gates, последовательный запуск адресных тестов и полного acceptance, fail-fast, JSON/Markdown-отчёты и коды `0/10/20/30/40/50`. `Build-Release.ps1` теперь по умолчанию делегирует выпуск gate; `-SkipGate` оставлен только для внутреннего шага упаковки.

**Проверка:** Verify с P5 и обработчиками изменения основания завершился `exit=0` в `Trash/release-gate-20260830-025544/report.md`; Release завершился `exit=0` в `Trash/release-gate-20260830-030504/report.md`, включая grouped-тест и полный acceptance. Актуальный артефакт `CreateOrderReleases/CreateOrder_Release_20260830_031131.xlsm` прошёл ZIP/Open XML и Excel read-only; SHA-256 `6145731EE72A6456E4670470E4E5BACBBA05693E9A4DEDCD1F852E65D446F29C`. Backup: `CreateOrderBackups/release-gate-20260830-030504/CreateOrder.xlsm.before-release-gate.xlsm`. Намеренный mismatch остановился на `source-book-sync` с `exit=20`; отсутствующая книга/неверный режим — `exit=10`.

**Ограничение:** `-SkipManual` явно фиксирует пропуск визуальной проверки форм/Word; это не доказательство ручной приёмки. Релизный файл создан из disposable-копии, `CreateOrder.xlsm` не пересобиралась и не перезаписывалась.

**Следующий конкретный шаг:** P5 реализован и включён в release-gate; ручная визуальная проверка формы и DOCX остаётся за оператором. P6 не запускается до письменного owner gate по юридическим правилам.

## P4 — центр истории и документов сотрудника завершён 29.08.2026

**Цель:** дать оператору единый read-only просмотр истории сотрудника с безопасными явными действиями над документами и корректировками.

**Сделано:** создана спецификация `.spec/tasks/p4-personnel-history-document-center.md`,
read-model `CreateOrder.xlsm.modules/mdlPersonnelHistoryCenter.bas` и
design-time форма `CreateOrder.xlsm.modules/frmPersonnelHistoryCenter.frm/.frx/.layout.csv`.
Форма ищет точный `EmployeeID`, личный/табельный номер или ФИО и показывает карточку,
состояние, события, snapshots, назначения, документы и `StaffStateSyncLog`.
Действия «Открыть документ», «Повторить экспорт» и «Подготовить корректировку»
вызываются только явными кнопками; browse/close не пишут в реестры. Добавлены
локализация и Ribbon-кнопка `openPersonnelHistoryCenter`.

**Проверка и внедрение:** `tools/Test-PersonnelHistoryCenterSafe.ps1` подтвердил
детерминированную хронологию, missing/ambiguous ошибки и отсутствие мутаций;
`tools/Test-PersonnelHistoryCenterDesigner.ps1` подтвердил 16 статических контролов
и отсутствие runtime-разметки; Ribbon-регрессия зелёная. Установщик завершён после
резервной копии `CreateOrderBackups/personnel-history-center-installed-20260829-234654`.
Финальный post-P4 release-gate подтвердил source/book sync и итоговый релизный
артефакт; `-SkipManual` оставил визуальную приёмку владельца явным
предупреждением.

**Следующий конкретный шаг:** P5 завершён и подключён к release-gate. Ручная
визуальная проверка формы/DOCX остаётся за оператором; следующий технический
этап — owner gate P6 по юридической модели статусов, ролей и переходов.

## P5 — единый многопараграфный приказ завершён 30.08.2026

Основанием реализации стала спецификация `.spec/tasks/p5-grouped-personnel-order.md`,
в которой зафиксированы образец приказа, правила группировки, нумерация,
регистрация и ручная визуальная приёмка.

- `CreateOrder.xlsm.modules/mdlGroupedPersonnelOrderExport.bas` — read-only
  построение отчёта, строгая проверка записей, группировка и экспорт DOCX.
- `CreateOrder.xlsm.modules/frmGroupedPersonnelOrderWizard.frm/.frx/.layout.csv`
  — design-time форма с предварительным просмотром и явным экспортом.
- `resources/customUI14.xml`, `mdlRibbonHandlers.bas`, `ModuleLocalization.bas`
  — кнопка Ribbon и локализованные подписи.
- `tools/Test-GroupedPersonnelOrderSafe.ps1` — disposable E2E с пятью событиями,
  негативной проверкой основания, регистрами и проверкой `word/document.xml`.
- `tools/Invoke-CreateOrderReleaseGate.ps1` — grouped-тест включён в общий gate.

В спецификацию P5 добавлен фактический инвентарь кандидатов DOCX/PDF с размерами
и SHA-256; утверждённым эталоном остаётся полный PDF владельца.

Владелец подтвердил `C:\Users\Nachfin\Desktop\ОбразецПриказа_распознан.pdf` как
утверждённый образец P5-Q1 целиком; весь PDF является эталоном отображения
шапки, кадрового блока, блока надбавок, основания и подписи. Версия/дата образца
не указана, хэш зафиксирован в спецификации.
P5-Q2 подтверждён: `ENROLLMENT`, `TRANSFER` и `EXCLUSION` допускаются в одном
документе, но отдельными `§` для каждой присутствующей аналогичной категории;
внутри каждого параграфа идут отдельные подпункты сотрудников (например, 5
зачислений, 2 исключения и 8 перемещений в трёх параграфах). Выплаты сохраняются
отдельными назначениями и никогда не объединяются между сотрудниками; лимит 100%
относится к особым достижениям, водительская надбавка начисляется сверх него, а
контрактное 60%-ное основание хранится отдельно, поэтому общий процентный итог
может быть выше 100%. Нумерация подтверждена и реализована: каждый документ
начинается с `§1`, сотрудники внутри параграфа — 1, 2, 3… Записи читаются в
порядке заведённых строк: параграфы получают номера по первому появлению
категории, сотрудники сохраняют порядок ввода. Для регистрации используется
`PERSONNEL_GROUPED_ORDER`/`EXPORTED` с первым `EventID` в legacy-поле и полным
списком в `DocumentEventLinks`. Выплаты разных сотрудников не объединяются ни
при каком совпадении. Выбранная выплата без обязательного основания подсвечивается
в V2 и блокирует общий экспорт; невыбранная выплата не проверяется. Статус
`REQUIRES_DECISION` блокирует экспорт, а допустимый итог выше 100% из-за
водительской надбавки только предупреждается.

Для передачи решений одним сообщением подготовлен шаблон
`docs/OWNER_GATE_P5_P6_RESPONSE_TEMPLATE.md`.

**Проверка и внедрение:** код и форма импортированы в закрытую рабочую книгу
после резервных копий; адресный P5-тест и общий Verify/Release зелёные. Ошибка
валидации не создаёт частичный DOCX и не добавляет строки в `DocumentRegistry`/
`DocumentEventLinks`; после успешного `SaveAs2` регистрируется один документ и
по одной связи на каждое событие. Готовый DOCX оператор открывает, визуально
сверяет с PDF-эталоном и печатает.

## P6 — жизненный цикл НПА и симулятор правил: owner gate

Спецификация `.spec/tasks/p6-legal-rule-lifecycle-simulation.md` подготовлена,
но реализация не начиналась. Нужны письменные решения владельца по вопросам
P6-Q1…P6-Q9: допустимые статусы и переходы, роли, обязательные реквизиты НПА,
интервалы действия, приоритеты конфликтующих правил, историческая дата расчёта,
поведение черновиков и точный формат read-only симуляции. До получения этих
решений код, книга и расчётные правила не изменяются.

## P3.1 — контракт единого release-gate зафиксирован 29.08.2026

**Сделано:** `.spec/tasks/p3-unified-release-gate.md` описывает Verify/Release, входы/выходы, коды возврата 0/10/20/30/40/50, preflight Office без завершения пользовательских процессов, сверку исходников с книгой, порядок адресных/полных тестов, Word/Open XML-проверки, JSON/Markdown-отчёты и stop rules без частичного выпуска.

**Следующий шаг:** P3.2–P3.4 и P4.1–P4.4 завершены; `Build-Release.ps1` остаётся
упаковщиком и не выпускает артефакт до зелёного gate. Финальный post-P4 gate
зелёный; следующий этап — owner gate P5.

## P1.5 — двухфазное сохранение подключено и внедрено 29.08.2026

**Цель:** исключить запись кадрового события до явного подтверждения оператором и сохранить ручную геометрию формы.

**Сделано:** `frmPersonnelActionWizardV2` собирает transient draft, строит read-only preview и переводит `MultiPage` на `pgPreview`. Только `ConfirmConfirmationPreview` переводит форму в `CONFIRMED` и вызывает существующий `SaveAction`; отмена очищает draft/result/signature и возвращает `EDITING`. Экспорт разрешён только в состоянии `SAVED`; повторное подтверждение после сохранения возвращает тот же EventID и не создаёт второе событие. Изменение поля после открытия preview сбрасывает устаревший preview. Добавлены локализованные статусы ready/invalid/changed/cancelled/confirm-required и безопасный установщик, который при существующей форме заменяет только код через `CodeModule.AddFromString` без служебных `Attribute`-строк, сохраняя designer/FRX и ручные координаты.

**Проверка и внедрение:** на disposable copy V2 E2E зафиксировал неизменность `PersonnelEvents`, snapshots, assignments и `DocumentRegistry` после Cancel, ровно одно событие после Confirm и отсутствие второго события после duplicate-confirm; затем проверил два DOCX. На рабочей книге последовательно прошли designer-test, preview smoke и `PERSONNEL_ACTION_V2_E2E_OK`. Рабочая книга обновлена после staging и backup `CreateOrderBackups/personnel-action-v2-installed-20260829-202814/CreateOrder.before-personnel-action-v2-install.xlsm`; owner-edited форма экспортирована с `geometryChanges=5` и сохранена в tracked `.frm/.frx/.layout.csv`.

**Следующий шаг:** P1.6 завершён технически; при желании владелец открывает preview визуально после ручной компоновки. P2 не начинать автоматически.

## P1.6 — автоматическая приёмка завершена 29.08.2026

**Результат:** технические проверки P1 завершены. Полная acceptance-регрессия на одноразовой копии завершилась `ACCEPTANCE_SMOKE_OK`; отдельно прошли `Test-PersonnelActionWizardSafe.ps1`, `Test-PersonnelEvents.ps1` и `Test-PersonnelRibbonSafe.ps1`. Рабочая книга уже содержит подтверждённый P1.5-код и экспортированную owner geometry.

**Ограничение:** автоматические проверки подтверждают структуру, компиляцию и поведение preview, но не заменяют субъективную визуальную проверку владельцем после последней косметической правки. P2 не начинается автоматически.

## P1.2 — read-only preview builder реализован и внедрён 29.08.2026

**Цель:** подготовить transient preview для `TRANSFER`/`EXCLUSION` до записи кадрового события.

**Сделано:** добавлен `CreateOrder.xlsm.modules/mdlPersonnelActionPreview.bas` с публичным `BuildPersonnelActionPreview(draftValues)`. Builder читает текущее состояние и активные назначения без `EnsurePersonnelEventInfrastructure`, строит `before`/`after`, детерминированный diff, прогноз `START`/`CONTINUE`/`STOP`/`REQUIRES_DECISION`, предупреждения, counts и `order_projection`. Запись в `PersonnelEventInput`, `PersonnelEvents`, snapshots, assignments, documents, current state, status и создание листов отсутствуют. Логи ограничены `DEBUG preview-built`/`ERROR preview-failed` без PII.

**Тест:** новый `tools/Test-PersonnelActionPreviewSafe.ps1` создал fixture на disposable copy, дважды построил TRANSFER preview, построил EXCLUSION preview и невалидный preview; проверил diff должности, `IsActive = NO` как прогноз, продолжение/остановку выплат и неизменность шести кадровых реестров.

**Внедрение:** staging и рабочая книга импортированы только при закрытых Excel/Word; рабочая книга сохранена после backup. Post-install smoke вызвал установленный модуль с невалидным типом события и подтвердил `can_confirm = False`; временный probe в рабочей книге не сохранялся.

**Ограничение:** кнопка Save формы всё ещё использует старый прямой `SaveAction`; P1.2 не подключает UI, а текстовая модель P1.3 не создаёт Word сама. Следующий шаг — design-time owner gate P1.4.

## P1.3 — общий DTO/text model preview и Word реализован и внедрён 29.08.2026

**Цель:** исключить расхождение между существенными строками preview и индивидуальным Word-приказом.

**Сделано:** добавлен чистый `CreateOrder.xlsm.modules/mdlPersonnelOrderText.bas`. `BuildPersonnelOrderTextModel` получает уже прочитанные `eventData`, `beforeState` и `afterState`, возвращает нормализованные реквизиты, даты, кадровые состояния, строки назначения, оклады, отпускно-финансовые строки и существенный текст TRANSFER/EXCLUSION. Builder P1.2 вкладывает эту модель в `order_projection.text_model`; Word-экспорт вызывает ту же модель через `WriteTransferOrderFromModel`/`WriteExclusionOrderFromModel`. Публичный API `ExportPersonnelEventOrder(eventID)` не изменён; создание Word, регистрация документа и перевод статуса остаются только в экспортёре.

**Проверка:** `tools/Test-PersonnelActionWizardV2Safe.ps1` построил preview до `SaveAction`, затем экспортировал два DOCX и проверил точное совпадение общего transfer core text, даты/номера, должности, ВУС и отпускно-финансовых строк. Existing V2 E2E и P1.2 read-only test зелёные.

**Внедрение:** `tools/Install-PersonnelActionPreviewSafe.ps1` импортировал три связанных модуля в staging и `CreateOrder.xlsm` при закрытых Excel/Word; backup: `CreateOrderBackups/personnel-action-preview-installed-20260829-182928/CreateOrder.before-personnel-action-preview-install.xlsm`.

**Owner gate P1.4:** решения владельца получены и применены: отдельная страница `MultiPage`, только существенные строки, after=before — предупреждение. Новая страница требует только необязательной ручной художественной подгонки.

## P1.4 — design-time экран предпросмотра подготовлен 29.08.2026

**Цель:** дать оператору отдельную страницу для проверки результата кадрового действия до будущего подтверждения сохранения.

**Сделано:** через штатный Excel Designer добавлена страница `pgPreview` в `mpAction` и 12 статичных элементов: заголовок, read-only/multiline поля `txt_preview_before`, `txt_preview_after`, `txt_preview_payments`, `txt_preview_warnings`, а также кнопки `btnPreviewConfirm` и `btnPreviewCancel`. Существующие контролы, имена, типы и контейнеры не менялись; runtime `Controls.Add` и присваивания `.Left/.Top/.Width/.Height` в коде отсутствуют. Добавлены локализационные ключи `personnel.preview.*` в `ModuleLocalization.bas` и лист `Localization`. Поверх этого экрана подключён двухфазный поток P1.5: кнопка сохранения открывает preview, а подтверждение вызывает запись ровно один раз.

**Проверка:** `tools/Test-PersonnelActionWizardV2Designer.ps1` подтвердил 80 строк manifest, три страницы (`pgTransfer`, `pgExclusion`, `pgPreview`), read-only поля, локализацию и отсутствие runtime-геометрии. После ручной компоновки владельца `tools/Export-PersonnelActionWizardV2Designer.ps1` экспортировал форму с `geometryChanges=5` и создал backup `CreateOrderBackups/personnel-action-v2-owner-layout-20260829-202943`. P1.2 read-only preview test и V2 E2E с двумя DOCX также зелёные.

**Owner gate:** ручная компоновка владельца выполнена. Дальше можно косметически править элементы `pgPreview` в VBE, не меняя `(Name)`, тип или контейнер; после такой правки нужно повторить экспорт и адресные проверки. P1.5 и автоматическая часть P1.6 завершены.

## P0 — кадровая форма V2 активирована и проверена 29.08.2026

**Результат:** рабочие команды меню, перемещения и исключения открывают `frmPersonnelActionWizardV2`. В `mdlPersonnelEvents` активны две ссылки на V2 и нет активных ссылок на V1; прежняя `frmPersonnelActionWizard` сохранена в книге как UserForm Type 3 для отката. Владелец разрешил завершить P0 на текущей базовой компоновке; экспорт подтвердил 68 элементов, две страницы и 0 изменений геометрии.

**Безопасное внедрение:** добавлен `tools/Activate-PersonnelActionWizardV2Safe.ps1` с INFO/DEBUG/ERROR-логами, пробной книгой, ожиданием штатного завершения Office и автоматическим восстановлением резервной копии при ошибке post-install проверки. Финальная копия перед переключением: `CreateOrderBackups/personnel-action-v2-activated-20260829-161815/CreateOrder.before-personnel-action-v2-activation.xlsm`.

**Проверка:** designer-тест подтвердил активную V2 и сохранённую V1; повторно прошли V2 E2E с двумя DOCX, прямая V1-регрессия, ribbon-тест и полный `Test-PaymentsEnrollmentAcceptance.ps1` с `ACCEPTANCE_SMOKE_OK`. Встроенный `mdlPersonnelEvents` совпадает с исходником. Рабочий маршрут открыт read-only: меню, перемещение и исключение показаны без сохранения данных; в видимой области явных наложений нет. После проверки Excel и Word закрыты.

**Следующий конкретный шаг:** P0 и P1 завершены технически. При желании владелец может визуально открыть preview после ручной компоновки; следующий приоритет — P2 по отдельной команде. Косметическое выравнивание V2 можно продолжать вручную с последующим экспортом и повторной QA.

## Текущий релиз — выпущен и проверен 29.08.2026

**Релизный файл:** `CreateOrderReleases/CreateOrder_Release_20260829_160203.xlsm`.

**Исправлено во время выпуска:** новые PowerShell-скрипты V2 получили UTF-8 с BOM и безопасное вычисление путей после `param`, поэтому они работают в Windows PowerShell; кадровый V2 E2E ожидает штатное завершение COM-процессов. В `Build-Release.ps1` исправлена пересборка Open XML: ZIP теперь содержит пути с `/`, а не `\`.

**Проверка:** полный `Test-PaymentsEnrollmentAcceptance.ps1` завершился `ACCEPTANCE_SMOKE_OK`; прошли V2-дизайнерские/логические тесты, кадровые safe/V2 E2E, компактная UI-, тарифная, ФИЗО-, медальная и ribbon-проверки. Выпуск проверен как ZIP (`[Content_Types].xml`, `xl/workbook.xml`, `xl/vbaProject.bin`, без обратных слэшей) и открыт Excel read-only: 29 листов, 61 VBA-компонент. Рабочая книга при выпуске не сохранялась.

**Примечание:** первая ошибочная копия релиза с обратными слэшами была создана только текущим запуском и перенесена в `Trash/rejected-releases/`; использовать её нельзя.

**Следующий конкретный шаг:** релизный состав уже закоммичен и отправлен в GitHub коммитом `0a49f52`; выпущенный файл остаётся снимком до активации кадровой V2. Текущее рабочее состояние описано в разделе P0 выше.

## Ближайший план улучшений — зафиксирован 29.08.2026

**Состояние:** приоритетный план используется как очередь; P0 и P1.1–P1.6 фактически закрыты, P2–P6 не начинались. Последующие изменения VBA и `CreateOrder.xlsm` выполняются отдельными этапами.

**Очередь:** P0 — завершить ручную компоновку и активировать `frmPersonnelActionWizardV2`; P1 — подтверждение «до → после»; P2 — центр целостности данных; P3 — единый release gate; P4 — центр истории и документов; P5 — многопараграфный/групповой приказ; P6 — жизненный цикл НПА и симулятор правил. P0–P4 являются ближайшим горизонтом, P5–P6 зависят от утверждённых образцов и юридических правил.

**План:** `.spec/tasks/createorder-improvement-priorities-2026-08-29.md`.

**Следующий конкретный шаг:** при желании владельца открыть preview визуально после ручной компоновки; затем следующий приоритет — P2, только по отдельной команде. P3–P6 без отдельной команды не начинаются.

## Дизайнерская форма перемещения и исключения V2 — подготовительный этап 29.08.2026

**Цель:** дать владельцу тот же ручной контроль расположения полей, который уже используется в `frmEnrollmentWizardV2`, и исключить настройку интерфейса через координаты в чате.

**Реализовано:** создана и установлена форма `frmPersonnelActionWizardV2` с 80 design-time элементами: отдельный контейнер меню, общий блок поиска/реквизитов, `MultiPage` со страницами `pgTransfer`, `pgExclusion` и `pgPreview`, общий статус, кнопки действий и read-only блок предпросмотра. Существующая логика поиска, transient preview, двухфазного подтверждения, сохранения `PersonnelEvents`, снимков, деактивации сотрудника и Word-экспорта подключена к статическим контролам. В коде V2 отсутствуют `Controls.Add` и присваивания `Left`, `Top`, `Width`, `Height`, поэтому ручная геометрия не будет восстановлена программно.

**Историческая безопасная граница:** на подготовительном этапе рабочие команды продолжали открывать `frmPersonnelActionWizard` V1. После завершения P0 маршрутизация переключена на V2, а V1 не удалена и остаётся откатным компонентом.

**Проверка:** `tools/Test-PersonnelActionWizardV2Designer.ps1` подтвердил UserForm Type 3, 80 уникальных элементов, три страницы, read-only preview, локализацию и загрузку режимов меню/перемещения/исключения. `tools/Test-PersonnelActionWizardV2Safe.ps1` в одноразовой копии создал тестовое зачисление, сохранил через V2 перемещение и исключение, сформировал оба DOCX, проверил изменение текущей должности, `Employees.IsActive = NO` и финансово-отпускные строки. Инсталлятор и экспортёр дополнительно проверены на отдельной копии; P1.5 backup: `CreateOrderBackups/personnel-action-v2-installed-20260829-202814/CreateOrder.before-personnel-action-v2-install.xlsm`, owner-layout backup: `CreateOrderBackups/personnel-action-v2-owner-layout-20260829-202943`.

**Следующий конкретный шаг:** подготовительный этап закрыт завершением P0. Будущие косметические правки выполняются вручную только по координатам/размерам с последующим экспортом и повторной QA; V2 уже является рабочим маршрутом.

## Legacy-БИК и индивидуальные кадровые приказы — исправлено и внедрено 28.08.2026

**Исправлено:** повторное сохранение старой карточки больше не очищает непустой БИК, когда справочник `BANK` не вернул значение; непустой справочный БИК по-прежнему заменяет сохранённый. `TRANSFER` и `EXCLUSION` теперь используют фактические дату/номер, общие настройки шапки, города и подписанта, реквизиты снимков до/после, ВУС, тариф и оклады. Для исключения в форме доступны материальная помощь, основной и дополнительный отпуск; заполненные значения выводятся в Word.

**Граница:** новые НПА, проценты и юридические факты не додумывались — Word печатает только сохранённые назначения и заполненные реквизиты. Групповые параграфы и единый многопараграфный приказ остаются отдельным будущим расширением, а не дефектом этого индивидуального маршрута.

**Проверка:** `tools/Test-PersonnelActionWizardSafe.ps1` проверяет не только сохранение/поиск, но и полный текст обоих DOCX: отсутствие старых плейсхолдеров, кадровые реквизиты, три даты перемещения, оклады, место убытия, финансово-отпускные поля, `ОСНОВАНИЕ:` и подпись. Проверка геометрии подтверждает, что новые поля исключения не перекрывают кнопки. Полный `Test-PaymentsEnrollmentAcceptance.ps1` до и после импорта завершился `ACCEPTANCE_SMOKE_OK`; встроенные модули книги сопоставлены с исходниками.

**Визуальная QA:** свежие transfer/exclusion DOCX преобразованы Word COM → PDF → PNG и просмотрены постранично. Оба документа одностраничные; наложений, обрезаний, выхода за поля и отрыва подписного блока нет. Материалы: `Trash/qa-personnel-order-fix-20260828/`.

**Внедрение:** проверенные `ModuleLocalization`, `mdlEnrollmentWorkflow`, `mdlPersonnelEventOrderExport` и `frmPersonnelActionWizard` импортированы в закрытую `CreateOrder.xlsm`. Три новые подписи присутствуют и во встроенной локализации, и в строках 651–653 листа `Localization`; адресный тест проверяет оба слоя. Финальная резервная копия перед повторным импортом и заполнением листа: `CreateOrderBackups/personnel-order-legacy-bik-fixed-20260829-034731/CreateOrder.before-personnel-order-legacy-bik-fix.xlsm`.

**Следующий конкретный шаг:** обязательных действий по двум дефектам нет. Следующее расширение — только по отдельному решению владельца: групповые параграфы/единый приказ либо дополнительные утверждённые юридические правила.

## Сквозная проверка кадрового маршрута — базовый маршрут пройден 28.08.2026

**Цель:** независимо подтвердить маршрут `зачисление → перемещение → исключение` после подключения дизайнерского мастера V2 и проверить три Word-приказа.

**Проверка:** в одноразовой копии `CreateOrder.xlsm` создан только тестовый сотрудник `QA E2E Employee`. `frmEnrollmentWizardV2` сохранила зачисление и сформировала приказ; `frmPersonnelActionWizard` сохранила перемещение и исключение, экспортировав ещё два приказа. Все три файла существуют, не пусты, содержат имя тестового сотрудника и соответствующие номера `QA-ENROLL-001`, `QA-TRANSFER-001`, `QA-EXCLUSION-001`; в `DocumentRegistry` есть связи с исходными событиями. После исключения `Employees.IsActive = NO`. Каждый приказ открыт в Word и просмотрен при 100%: по одной странице, без видимых наложений или обрезаний. Артефакты сохранены только в игнорируемом `Trash\qa-e2e-personnel-v2-20260828\run-20260828-203911\`; рабочая книга и реальные данные не менялись.

**Регрессия после исправления:** `tools/Test-PersonnelActionWizardSafe.ps1` и полный `Test-PaymentsEnrollmentAcceptance.ps1` повторно пройдены; legacy-resave сохраняет БИК, а оба кадровых DOCX проходят структурную и визуальную проверку.

**Следующий шаг:** маршрут готов к эксплуатации в пределах реализованного индивидуального сценария.

## Дизайнерский мастер зачисления V2 — завершено 28.08.2026

**Цель:** сделать рабочий мастер визуально редактируемым в конструкторе VBA без согласования числовых координат в чате.

**Реализовано:** владелец вручную скомпоновал `frmEnrollmentWizardV2`; экспорт подтвердил 300 неизменённых элементов, семь страниц и 79 изменений координат/размеров. К design-time контролам подключены 167 связей бизнес-логики и 133 локализуемые подписи. В коде V2 нет `Controls.Add` и присваиваний `Left`, `Top`, `Width`, `Height`, поэтому сохранённая в дизайнере геометрия не переопределяется при открытии. `mdlEnrollmentWorkflow` и `mdlRibbonHandlers` переключены на V2; прежняя `frmEnrollmentWizard` сохранена в книге как резервный компонент.

**Проверка и внедрение:** до установки и после неё `tools/Test-EnrollmentWizardV2Logic.ps1` подтвердил компиляцию, загрузку семи страниц и рабочие подписи кнопок. После установки прошли `tools/Test-EnrollmentWizardV2Designer.ps1` (`V1 retained, V2 active`), `tools/Test-EnrollmentCompactUiSafe.ps1` (регрессия V1), `tools/Test-EnrollmentTariffReferenceSafe.ps1` и полный `Test-PaymentsEnrollmentAcceptance.ps1` с результатом `ACCEPTANCE_SMOKE_OK`. В live-проверке рабочая команда открыла V2; вкладки «Военнослужащий» и «Ежемесячные выплаты» отрисовались с ручной компоновкой, переключение прошло без ошибки, данные не сохранялись. Резервная копия перед активацией: `CreateOrderBackups/enrollment-designer-v2-installed-20260828-185918/CreateOrder.before-enrollment-designer-v2-activation.xlsm`.

**Следующий шаг:** обязательных технических шагов по V2 не осталось. Для дальнейшей косметической правки владелец может перемещать/масштабировать существующие контролы V2 в дизайнере, не меняя `(Name)` и структуру; после сохранения и закрытия Excel нужно повторить экспорт и адресные проверки. Сквозной сценарий `зачисление → перемещение → исключение` с тремя Word-приказами пройден 28.08.2026 в изолированной копии.

## Регрессия мастера зачисления — 27.08.2026

**Цель:** восстановить достоверность автоматической приёмки и устранить потери данных в мастере.

**Реализовано:** ФИЗО приведено к действующей шкале `SECOND/FIRST/HIGH = 80/90/100`; добавлены восемь недостающих локализационных ключей. Самовосстановление листов больше не доверяет устаревшему кэшу, страница дополнительных выплат прокручивается до последнего поля, а банковские реквизиты старых строк сохраняются без отдельного флага включения.

**Дополнительно исправлено 28.08.2026:** старые строки с заполненными документами, персональными или банковскими реквизитами автоматически восстанавливают соответствующие флажки при загрузке. Это предотвращает очистку уже существующих данных при повторном сохранении. Матрица полной приёмки синхронизирована с действующими блоками выплат и автоматическими окладами из справочника.

**Визуальная приёмка и последнее исправление 28.08.2026:** реальное открытие рабочей формы выявило сырой плейсхолдер `{index}` в подписях вкладки «Иные выплаты». Конструктор комбобоксов теперь подставляет номер строки при создании контрола, а acceptance-проба читает фактические подписи формы вместо отдельно вычисленного ожидаемого текста. В live-форме подтверждены `Ежемес. #1…#4` и `Разовая #1…#3`; страницы документов, ежемесячных выплат, реквизитов и иных выплат визуально помещаются без обрезания, штатная прокрутка доступна.

**Проверка и внедрение:** до импорта и после него полный `Test-PaymentsEnrollmentAcceptance.ps1` завершился `ACCEPTANCE_SMOKE_OK`. После импорта также прошли `Test-EnrollmentFizoReferenceSafe.ps1`, `Test-EnrollmentTariffReferenceSafe.ps1`, `Test-EnrollmentMedalReferenceSafe.ps1`, `Test-EnrollmentCompactUiSafe.ps1` и `tools/verify_geometry.py`. В закрытую `CreateOrder.xlsm` импортированы `ModuleLocalization`, `mdlEnrollmentWorkflow` и `frmEnrollmentWizard`; резервная копия основной регрессии: `CreateOrderBackups/enrollment-regression-complete-20260828-010732/CreateOrder.before-enrollment-regression-complete.xlsm`. Перед финальным исправлением подписей создана дополнительная копия `CreateOrderBackups/enrollment-extra-labels-20260828-012742/CreateOrder.before-enrollment-extra-labels.xlsm`; после импорта формы адресный тест прошёл за `0,499 с`, полный acceptance повторно завершился `ACCEPTANCE_SMOKE_OK`.

**Закрытая регрессия 28.08.2026:** усиленная проверка legacy-resave подтвердила сохранность всех проверяемых полей, включая БИК при пустом результате справочника `BANK`. Непустой справочный результат по-прежнему имеет приоритет.

**Следующий шаг:** обязательных действий по регрессии нет.

## Безопасная уборка временных артефактов — 09.08.2026

**Цель:** убрать из корня проекта накопившиеся тестовые копии книги, не удаляя возможность восстановить материалы старых прогонов.

**Реализовано:** четырнадцать папок `_tmp_*`, созданных автоматическими проверками VBA, и пустая устаревшая папка `old_form_modules` перенесены в `Trash\2026-08-09_test-sandboxes`. В архив добавлен `README.md` со списком и инструкцией по восстановлению. В корне больше нет папок `_tmp_*`. Добавлена [карта папок](WORKSPACE_LAYOUT.md), объясняющая назначение рабочих исходников, резервных копий, релизов, диагностики и архива.

**Сохранено без изменений:** `CreateOrderBackups` (действующие копии до импортов VBA), `CreateOrderReleases`, `CreateOrder.xlsm.modules`, `tools`, `tmp`, `workbook-modules`, шаблоны, итоговые Word-документы и отдельный сторонний проект `antigravity_glm_mcp`.

**Проверка:** все переносы проверены по абсолютным путям внутри рабочего каталога; `git status` не показывает удаления отслеживаемых файлов. Спецификация: `.spec/tasks/workspace-artifact-archive-2026-08-09.md`.

**Следующий шаг:** при необходимости отдельно согласовать политику хранения для старых копий в `CreateOrderBackups` и материалов `Trash`; до такого решения ничего из них не удалять.

## Подгонка ширин полей по визуальной приёмке — 19.07.2026

**Цель:** уменьшить ширину полей `%` и комбобоксов, которые на скриншоте выглядели слишком крупными по отношению к их содержимому, и опустить строку «Премия» пониже, чтобы был воздух между ней и строкой «Секретность/Классность».

**Реализовано (раунд 1, только ширины):** `CTRL_PERCENT_WIDTH` 52 → 28 (все поля `%` стали уже, потому что значение до `100%` помещается в 28 px), `CTRL_PARAM_WIDTH` 136 → 96 (комбобоксы секретности/классности/ФИЗО), `CTRL_PARAM_ACHIEVEMENT_WIDTH` 174 → 120 (комбобокс медали), `FRA430_COL_RIGHT_AMOUNT_WIDTH` 70 → 36 (поле «% / сумма» медали), `CTRL_DOC_WIDTH` 192 → 160 (поле «Номер приказа»), `FRA727_ROW3_CHK_TOP` 114 → 126 (Премия опустилась ниже секретности). Колонки (Left) не сдвигались — элементы стали уже на тех же местах, без перетасовки.

**Проверка:** `Test-EnrollmentCompactUiSafe.ps1` прошёл за `0,477 с`. `Install-EnrollmentCompactUiSafe.ps1` сделал preflight (`Test-EnrollmentTariffReferenceSafe.ps1` + `Test-EnrollmentCompactUiSafe.ps1`), резервную копию `CreateOrderBackups\enrollment-compact-ui-installed-20260719-221145\CreateOrder.before-enrollment-compact-ui.xlsm`, импортировал форму как UserForm Type 3.

**Что НЕ менялось в этом раунде:** подписи чекбоксов («Надбавка по должности» и т. д.) — они приходят из листа `Localization`, отдельная задача. Расположение колонок (Left) — если визуально нужно сдвинуть секретность/ФИЗО ближе или дальше друг от друга, скажете — сделаю следующим раундом.

**Следующий шаг:** открыть `CreateOrder.xlsm`, мастер зачисления, вкладку «Ежемесячные выплаты», посмотреть результат. Если что-то не так — сказать «уже/шире/левее/правее/ниже» для конкретного контрола, я поправлю точечно.

## Вынос геометрии в константы — 19.07.2026

**Цель:** дать владельцу возможность править геометрию вкладки «Ежемесячные выплаты» без чтения тела `CreateMonthlyPage` и без безопасного импорта формы.

**Реализовано:** в начале `frmEnrollmentWizard.frm` добавлен блок `Private Const` с 47 именованными константами для двух фреймов (`fraOrder727`, `fraOrder430`), стандартных размеров контролов и отступов по строкам. Тело `CreateMonthlyPage` переписано так, что каждое магическое число в вызовах `AddPageFrame`/`AddPageCheckBoxT`/`AddPageTextBoxT`/`AddPageComboBoxT`/`AddLabelToPage` заменено на семантическую константу (`FRA727_LEFT`, `FRA727_ROW1_CHK_TOP`, `CTRL_PERCENT_WIDTH`, `FRA430_COL_DATE`, …). Логика и визуальная геометрия не изменились — все 31 присваивание проверены скриптом `tools/verify_geometry.py` и дают идентичные значения исходным числам.

**Проверка:** `Test-EnrollmentCompactUiSafe.ps1` прошёл за `0,492 с` (поведение формы побайтово совпадает с предыдущим). `Install-EnrollmentCompactUiSafe.ps1` сделал preflight (`Test-EnrollmentTariffReferenceSafe.ps1` + `Test-EnrollmentCompactUiSafe.ps1`), резервную копию `CreateOrderBackups\enrollment-compact-ui-installed-20260719-214443\CreateOrder.before-enrollment-compact-ui.xlsm` и импортировал форму как UserForm Type 3.

**Следующий шаг:** владелец правит числовые константы в начале `frmEnrollmentWizard.frm` (например, двигает `FRA727_ROW1_TXT_TOP` или меняет `FRA430_HEIGHT`) и запускает `Install-EnrollmentCompactUiSafe.ps1` — больше не нужно читать `CreateMonthlyPage`, чтобы понять, какое число за что отвечает. Изменение поведенчески не видимо пользователю, `UserGuide.md` не правился.

## Корректировка сетки выплат №430дсп и чистка рабочего дерева — 19.07.2026

**Цель:** завершить визуальную приёмку сетки выплат по замечанию оператора (дата и номер приказа о медали были срезаны нижней границей фрейма №430дсп) и прибрать служебный мусор в рабочем дереве.

**Реализовано:** фрейм №727 уменьшен 184→168, фрейм №430дсп увеличен 152→174 и сдвинут 248→232. Контролы ФИЗО/«Особые достижения» подняты на 8 пунктов, поля «Дата приказа» и «Номер приказа» медали перенесены в собственную строку фрейма №430дсп (126) и расширены до читаемой ширины. Все размеры и отступы по-прежнему задаются только программно в `CreateMonthlyPage`; ручное визуальное редактирование UserForm не используется. В рабочем дереве удалены три устаревших `.bas` (`ModuleLocalization.bas`, `mdlPersonnelEvents.bas`, `mdlPersonnelEventOrderExport.bas`) — их содержимое уже сохранено в `CreateOrder.xlsm.modules/` (актуальная версия) и `tmp/old_form_modules/` (исторический снимок). Папка `tmp/` (диагностические артефакты экспорта) добавлена в `.gitignore`, чтобы не засорять `git status`.

**Проверка и внедрение:** `Install-EnrollmentCompactUiSafe.ps1` создал резервную копию `CreateOrderBackups\enrollment-compact-ui-installed-20260719-174833\CreateOrder.before-enrollment-compact-ui.xlsm`, прошёл preflight (`Test-EnrollmentTariffReferenceSafe.ps1` и `Test-EnrollmentCompactUiSafe.ps1` — `0,559 с`), импортировал `frmEnrollmentWizard` как UserForm Type 3 и сохранил книгу. После импорта тесты прогнаны повторно: `Test-EnrollmentCompactUiSafe.ps1` — `0,492 с` (новая проверка вложенных контролов подтвердила отсутствие обрезаний), `Test-EnrollmentTariffReferenceSafe.ps1` — оба режима. Excel/Word процессов после установки не осталось.

**Следующий шаг:** ручная визуальная приёмка мастера зачисления без Computer Use — открыть вкладку «Ежемесячные выплаты», убедиться, что строка медали полностью видна внутри фрейма №430дсп, и подтвердить, что №727 остаётся компактным без обрезания премии и секретности. Если в визуальной приёмке выявятся новые замечания, корректировать только числовые координаты в `CreateMonthlyPage`.

## Компактная сетка выплат №727 и №430дсп — 18.07.2026

**Цель:** сделать вкладку «Ежемесячные выплаты» плотной и читаемой, чтобы процент находился непосредственно рядом с выбранной надбавкой, а состав фреймов совпадал с итоговым Word-приказом.

**Реализовано:** классность перенесена из фрейма №430дсп в фрейм №727. Её `word_legal_group` и предпросмотр Word переведены в `MO_727`; устаревшая формулировка пункта №430 для классности очищается при подготовке инфраструктуры. В №727 выплаты организованы в компактные строки: должность и особые условия, затем секретность и классность, затем премия. В №430дсп остались контракт, автоматический 1–4 тариф, ФИЗО и особые достижения/медаль. Поля процентов имеют ширину для `100%`, короткие списки классности и ФИЗО ограничены 136 пунктами, а поле медали сохранено шире для читаемого выбора. Страница выплат использует штатную вертикальную прокрутку MSForms и автоматически вычисляет высоту содержимого; внешние Windows-хуки для колеса мыши не применяются, чтобы не создавать риск зависания Excel.

**Проверка и внедрение:** на изолированных копиях прошли `Test-EnrollmentTariffReferenceSafe.ps1` и `Test-EnrollmentCompactUiSafe.ps1` (загрузка формы — 0,543 с). Второй тест дополнительно проверяет группу классности `MO_727`, размеры фреймов и прокрутку только страницы выплат. После проверки обновления сразу импортированы в `CreateOrder.xlsm`; резервная копия: `CreateOrderBackups\enrollment-compact-ui-installed-20260718-130310\CreateOrder.before-enrollment-compact-ui.xlsm`.

**Следующий шаг:** открыть мастер зачисления, выбрать вкладку выплат и визуально подтвердить удобство сетки; при необходимости уточнить только координаты отдельных полей, без изменения логики расчёта.

## Нормативные разделы Word и предзапуск VBA — 18.07.2026

**Цель:** строить результативную часть приказа так же, как в образце: не смешивать выплаты разных НПА, а создавать самостоятельные преамбулы и списки для №727, №430дсп и иных актов; не допускать в рабочую книгу элементарные ошибки VBA.

**Реализовано:** экспортёр собирает выбранные выплаты в отдельные блоки. Блок №727 появляется только при выбранных надбавках по должности, особых условиях, секретности или классности; блок №430дсп — только для 1–4 тарифа, контрактной надбавки, ФИЗО и медали. Для каждой типовой выплаты на `Настройки` хранятся группа НПА и, при необходимости, формулировка пункта; уже подтверждённые пункты №430дсп заполнены по умолчанию и могут быть отредактированы оператором. Поле «Приказ / пункт» иной ежемесячной выплаты теперь образует самостоятельную преамбулу `В соответствии с …, установить:` и группирует все выплаты с одинаковым значением. Индивидуальное подтверждение сохраняется в карточке и печатается только при включённой выплате; для автоматического 1–4 тарифа оно не требуется.

**Защита перед импортом:** `Install-EnrollmentCompactUiSafe.ps1` теперь до открытия рабочей книги запускает `Test-EnrollmentTariffReferenceSafe.ps1` и `Test-EnrollmentCompactUiSafe.ps1` на одноразовых копиях. Пробник использует `Option Explicit`, объявляет объектные переменные, проверяет их создание через `Set … Is Nothing`, а все исключения возвращает как текст вместо остановки VBE. Экспортёр также безопасно использует резервный текст, если кэш локализации не готов.

**Проверка и внедрение:** `Test-EnrollmentTariffReferenceSafe.ps1` подтверждает обе нормативные преамбулы, принадлежность 1–4 тарифа №430дсп, вывод индивидуального подтверждения ФИЗО и самостоятельный НПА-раздел `TEST-ACT-7`. `Test-EnrollmentCompactUiSafe.ps1` подтвердил загрузку формы и её структуру за 0,543 с. Оба теста автоматически выполняются установщиком до открытия рабочей книги. После этого обновление импортировано в `CreateOrder.xlsm`; резервная копия: `CreateOrderBackups\enrollment-compact-ui-installed-20260718-121402\CreateOrder.before-enrollment-compact-ui.xlsm`.

**Следующий шаг:** вручную сформировать Word на одной карточке с выплатой №727, одной/несколькими выплатами №430дсп и, при необходимости, иной ежемесячной выплатой; сверить порядок абзацев и юридические формулировки с утверждаемым образцом.

## Автоматический 1–4 тариф и перестройка вкладок — 16.07.2026

**Цель:** убрать из оператора ручные действия по надбавке 1–4 тарифных разрядов, оставить на вкладке оснований только действительно индивидуальные документы и сделать две вкладки ровными без нижнего обрезания.

**Реализовано:** `std_tariff_enabled` теперь всегда вычисляется из выбранного на первой вкладке разряда: 1–4 — `YES`, иной разряд — `NO`. В форме строка показывает результат и процент, но ручного флажка нет. Льготная выслуга вынесена отдельной строкой над приказом №727, включена по умолчанию и выводится самостоятельным Word-абзацем. Из вкладки оснований убраны и при сохранении очищаются поля льготной выслуги, надбавки по должности, особых условий и 1–4 тарифа: они больше не требуют индивидуального документа и не добавляются в Word. На вкладке оснований оставлены секретность, премия, контракт/430дсп, классность, ФИЗО, медаль и разовые выплаты. Вкладки «Документы и даты» и «Ежемесячные выплаты» переразмечены компактнее: нижние поля подняты, а элементы выплат расположены ровными строками.

**Проверка и внедрение:** `Test-EnrollmentCompactUiSafe.ps1` пройден за 0,500 с. `Test-EnrollmentTariffReferenceSafe.ps1` проверяет обе ветви: разряд 1 автоматически включает надбавку, разряд 5 автоматически выключает её. Модули сразу импортированы в `CreateOrder.xlsm`; резервная копия: `CreateOrderBackups\enrollment-compact-ui-installed-20260716-011503\CreateOrder.before-enrollment-compact-ui.xlsm`.

**Следующий шаг:** оператору вручную выбрать на первой вкладке `1 т.р.`, затем `5 т.р.` и убедиться, что строка 1–4 тарифа меняет состояние без ручного действия; проверить, что на двух переразмеченных вкладках нижние поля не обрезаны.

## Исправление обрезания вкладок и оснований выплат — 16.07.2026

**Цель:** сделать полностью видимыми поля на вкладках «Документы и даты» и «Основания выплат», а также обеспечить фактическое попадание индивидуального основания выплаты в Word.

**Реализовано:** увеличена высота блоков «Прибытие и предписание», «Рапорт и регистрация», «Кадровые даты» и «Общие основания»; строки «Предписание / основание» и «Рапорт / регистрация» больше не обрезаются. На вкладке оснований увеличены фреймы №727 и №430дсп, а блок разовых выплат сдвинут вниз: видны поля премии и особых достижений. Экспортёр теперь добавляет текст индивидуального основания только к абзацу включённой выплаты в Word, например: `Основание выплаты: …`. Это не включает выплату само по себе: назначение определяется её флажком.

**Проверка и внедрение:** повторно пройдены `Test-EnrollmentCompactUiSafe.ps1` (подготовка формы — 0,475 с) и `Test-EnrollmentTariffReferenceSafe.ps1`. Второй тест дополнительно проверяет, что включённая ФИЗО с индивидуальным основанием показывает его в Word-предпросмотре. После проверки модули сразу импортированы в `CreateOrder.xlsm`; резервная копия: `CreateOrderBackups\enrollment-compact-ui-installed-20260716-004650\CreateOrder.before-enrollment-compact-ui.xlsm`.

**Следующий шаг:** обычная ручная проверка двух вкладок в рабочей книге без Computer Use: убедиться, что все нижние поля видны и в Word после выбранной выплаты появляется её основание.

## Управляемые блоки документов и корректная группировка выплат — 16.07.2026

**Цель:** позволить не включать в приказ сведения о прибытии и документах, если они не нужны для конкретного зачисления; выровнять интерфейс и распределение выплат по приказам.

**Реализовано:** на вкладке «Документы и даты» реквизиты приказа, прибытие/предписание, рапорт/регистрация и кадровые даты оформлены отдельными фреймами. Блоки «Внести сведения о прибытии» и «Внести сведения о рапорте» работают так же, как личные и банковские реквизиты: выключенный блок очищается из записи, не валидируется и не включается в Word. Вкладка оснований также разбита на фреймы №727, №430дсп и разовых выплат; текст из поля основания включается в Word только при включённой соответствующей выплате. На вкладке ежемесячных выплат выровнена сетка; дата и номер приказа о медали помещаются в пределах блока. По согласованной классификации надбавка 1–4 тарифных разрядов перенесена в №430дсп, секретность — в №727.

**Проверка и внедрение:** `Test-EnrollmentCompactUiSafe.ps1` проверяет фреймы документов и направления выплат, `Test-EnrollmentTariffReferenceSafe.ps1` — что выключенные сведения не попадают в Word-предпросмотр. Оба теста пройдены; обновление сразу импортировано в `CreateOrder.xlsm`. Резервная копия: `CreateOrderBackups\enrollment-compact-ui-installed-20260716-003558\CreateOrder.before-enrollment-compact-ui.xlsm`.

**Следующий шаг:** ручная проверка оператором новой сетки без Computer Use.

## Чистый справочник и необязательные реквизиты зачисления — 16.07.2026

**Цель:** не превращать `EnrollmentReferenceData` в копию листа «Штат» и дать сформировать приказ до внесения реквизитов, которые могут уже быть в другой учётной системе.

**Реализовано:** из `mdlEnrollmentWorkflow` полностью удалена синхронизация должностей, ВУС, разделов и воинских частей со «Штатом». При подготовке справочника прежние строки `POSITION`, `VUS`, `SECTION` и `MILITARY_UNIT` удаляются; остаются только расчётные данные — оклады, размеры выплат, категории и банковские реквизиты. На первой вкладке мастера убрано дублирующее поле «Воинская часть»: сохраняется один «Раздел персонала». На вкладке реквизитов добавлены флажки «Внести персональные данные» и «Внести банковские реквизиты». При выключенном флажке пустые паспорт, ИНН, СНИЛС и счёт не блокируют проверку и Word; при включённом — соответствующий блок становится обязательным и проходит форматную проверку.

**Проверка и внедрение:** `tools/Test-EnrollmentCompactUiSafe.ps1` подтверждает отсутствие четырёх типов штатных строк, наличие флажков и загрузку формы за 0,502 секунды. `tools/Test-EnrollmentTariffReferenceSafe.ps1` подтверждает подстановку окладов, свободный экспорт без включённых реквизитов и обязательность данных после включения личного блока. Проверенные исходники сразу импортированы в `CreateOrder.xlsm`; резервная копия: `CreateOrderBackups\enrollment-compact-ui-installed-20260716-000859\CreateOrder.before-enrollment-compact-ui.xlsm`.

**Следующий шаг:** открыть форму и при необходимости включить один из двух блоков реквизитов; если он не нужен для конкретного зачисления, оставить флажок снятым. Визуальная приёмка не запускалась через Computer Use.

## Справочник окладов по постановлению Правительства №464 — 15.07.2026

**Цель:** дать мастеру зачисления проверяемые суммы для автоматической подстановки оклада по званию и тарифному разряду, не заменяя пользовательские правки.

**Реализовано:** `EnrollmentReferenceData.Amount` заполнен для 50 тарифных разрядов и общевойсковых званий от рядового до полковника значениями на 01.10.2025. Источник: коэффициент 1,076 из постановления Правительства РФ от 09.04.2025 №464 в редакции постановления №1390 и базовые перечни из приложений №2 и №3 к приказу МО РФ №727. `mdlEnrollmentWorkflow` записывает эти значения только в пустые ячейки, поэтому вручную установленная оператором сумма остаётся без изменений. Подготовка справочника удаляет дубли и не обращается к «Штату».

**Проверка и внедрение:** пройдены `tools/Test-EnrollmentTariffReferenceSafe.ps1` и `tools/Test-EnrollmentCompactUiSafe.ps1`; структурная проверка рабочей книги подтвердила 50/50 заполненных тарифов (`1 = 14 330`, `50 = 64 473`) и 15/15 званий (`рядовой = 7 166`, `полковник = 18 629`). Исходники и форма импортированы в `CreateOrder.xlsm`; резервная копия: `CreateOrderBackups\enrollment-compact-ui-installed-20260715-234144\CreateOrder.before-enrollment-compact-ui.xlsm`.

**Следующий шаг:** при открытой форме выбрать звание и тарифный разряд и убедиться, что оба поля оклада заполнились; при необходимости изменить конкретную сумму непосредственно в `EnrollmentReferenceData.Amount`.

## Компактный мастер зачисления и скорость — 15.07.2026

**Цель:** убрать вертикальные «портянки» на всех семи вкладках мастера, исключить повторную тяжёлую синхронизацию при открытии и экспорте, а также сделать понятными путь Word и причину пустых окладов.

**Сделано в исходниках:** `frmEnrollmentWizard` переразмечен в пределах одного окна: «Документы и даты» разделены на реквизиты приказа, документы прибытия, даты и основания; «Иные выплаты» сжаты до коротких строк; «Проверка и текст приказа» показывает полный путь созданного `.docx`. У всех вкладок отключена вертикальная прокрутка. Индикатор подготовки Word показывается непосредственно перед экспортом, а не при загрузке сотрудника. `mdlEnrollmentWorkflow` подготавливает инфраструктуру и справочник только один раз за сеанс; перед синхронизацией удаляет повторяющиеся строки `EnrollmentReferenceData`. Это устраняет причину, по которой справочник разросся тысячами одинаковых званий и замедлял открытие формы.

**Оклады и Word:** поля окладов читают `Amount` для `RANK` и `TARIFF_RANK`; изолированный тест подтверждает оба варианта поиска — по краткой подписи и коду. Значения на 01.10.2025 внесены в справочник отдельным подтверждённым обновлением выше и не перезаписывают ручные правки. Word-приказ сохраняется в папку, где лежит `CreateOrder.xlsm`; точный путь после успеха показан на вкладке проверки. Долгое создание Word дополнительно зависит от запуска Word и сохранения шаблона; повторная синхронизация справочника из этого пути исключена.

**Проверка и внедрение:** после закрытия Excel проверенные модули импортированы в рабочую `CreateOrder.xlsm` безопасным скриптом `tools/Install-EnrollmentCompactUiSafe.ps1`. До импорта создана резервная копия `CreateOrderBackups\enrollment-compact-ui-installed-20260715-224706\CreateOrder.before-enrollment-compact-ui.xlsm`. Повторно пройдены `tools/Test-EnrollmentCompactUiSafe.ps1` (0,531 секунды на подготовку VBA и загрузку формы) и `tools/Test-EnrollmentTariffReferenceSafe.ps1`. Следующий шаг — пользовательская визуальная приёмка формы; Computer Use для неё не используется.

## Группировка выплат по приказам и немодальная форма — 15.07.2026

**Реализовано:** на вкладке ежемесячных выплат установлены отдельные фреймы «Приказ МО РФ № 727» и «Приказ МО РФ № 430дсп»; контрактная 430-дсп выплата перенесена в свой фрейм. Вкладка иных выплат теперь требует указать «Приказ / пункт» для ежемесячной дополнительной выплаты, а для разовой — «Приказ / основание». Эти реквизиты добавляются к строке в Word-приказе. Мастер зачисления открывается немодально: оператор может перейти к `EnrollmentReferenceData`, заполнить `Amount` и вернуться к той же незакрытой карточке.

**Данные:** подтверждённые программные значения (стандартные выплаты, ФИЗО и медали) остаются заполненными. Оклады по званию и тарифным разрядам на 01.10.2025 внесены на основе постановления №464 и приложений к приказу №727; оператор может изменить конкретную строку `EnrollmentReferenceData.Amount`, и автоматическое заполнение не заменит её.

**Проверка:** безопасный тест формы дополнительно проверяет наличие обоих фреймов и безопасную компиляцию Word-экспортёра; `Test-EnrollmentCompactUiSafe.ps1` и `Test-EnrollmentTariffReferenceSafe.ps1` пройдены. Модули формы, workflow и Word-экспорта импортированы в `CreateOrder.xlsm`; резервная копия — `CreateOrderBackups\enrollment-compact-ui-installed-20260715-232704\CreateOrder.before-enrollment-compact-ui.xlsm`.

## Компактные кадровые формы — 15.07.2026

**Цель:** сделать перемещение и исключение понятными оператору, без технического листа и знания `EmployeeID`.

**Реализовано:** `frmPersonnelActionWizard` показывает компактные, различающиеся по составу поля для перемещения и исключения. Поиск находит фрагмент ФИО, личный, табельный и внутренний номер; совпадения появляются автоматически после двух введённых символов, а кнопка «Найти и загрузить» загружает единственную карточку. Повторная подготовка кадровых листов исключена: инфраструктура создаётся один раз за сеанс Excel. Исправлена критическая ошибка импорта формы: из-за LF-окончаний `.frm` раньше попадал в книгу как обычный модуль и выдавал `Invalid outside procedure`; теперь импорт подтверждён как UserForm `Type=3`.

**Основание и Word:** поле «Основание» сохраняется только после явной команды «Сохранить» в `PersonnelEvents.BasisText`; несохранённая изменённая форма предупреждает при закрытии. При «Сформировать Word» этот текст включается в приказ, а путь к `.docx` регистрируется в `DocumentRegistry` и показывается в строке состояния Excel.

**Проверка:** на изолированной копии книги пройдены `powershell -ExecutionPolicy Bypass -File .\tools\Test-PersonnelActionWizardSafe.ps1` и `powershell -ExecutionPolicy Bypass -File .\tools\Test-PersonnelRibbonSafe.ps1`. Тест проверяет импорт UserForm, живой поиск, сохранение и Word-экспорт перемещения и исключения. Резервная копия перед импортом: `CreateOrderBackups\personnel-action-autosearch-installed-20260715-221426\CreateOrder.before-personnel-action-autosearch.xlsm`.

**Следующий шаг:** визуальная приёмка оператором в удобный момент; пока она сознательно не запускается через Computer Use по просьбе пользователя.

## Обновление интерфейса зачисления — 15.07.2026

**Цель:** убрать неясности из формы зачисления и сделать справочные значения и Word-экспорт наблюдаемыми оператором.

**Реализовано:** ФИЗО приведено к набору 2 уровень — 80%, 1 уровень — 90%, высший уровень — 100%; в карточку и Word добавлен БИК. Банк выбирается из типа `BANK` листа `EnrollmentReferenceData`, БИК берётся из четвёртого столбца. Паспортные и банковские данные разделены, технические листы скрываются, а действия формы сокращены до проверки, сохранения, Word-экспорта и пакетного продолжения. Справочник получает общевойсковые звания от рядового до полковника, независимые от листа `Штат`.

**Открыто:** значения на 01.10.2025 уже внесены в `EnrollmentReferenceData`. Оператор при необходимости корректирует только `Amount` конкретной строки; новая синхронизация эти ручные значения не заменяет.

**Проверка:** после импорта в рабочую книгу повторно пройден `Test-PersonnelEvents.ps1` (15.07.2026). Структурная проверка `CreateOrder.xlsm` подтверждает ФИЗО 80/90/100 и скрытие шести технических листов. `Test-PaymentsEnrollmentAcceptance.ps1` прошёл все этапы до финального Word-группового экспорта, затем был остановлен внешним 120-секундным лимитом без зафиксированного функционального сбоя; повтор с лимитом 300 секунд завис в Office и был безопасно прекращён. Нужны полный прогон и визуальная приёмка на копии книги. Граф кода обновлён `graphify update .`. См. `.spec/tasks/enrollment-operator-clarity-2026-07-15.md`.

## Общая картина

`CreateOrder.xlsm` — Excel/VBA-приложение для подготовки приказов, работы с данными штата, надбавками и экспортом документов Word/Excel.

Базовые действующие сценарии: ДСО, выгрузки, надбавки без периодов, импорт Word-рапортов, проверка Д89, лицензирование и локализация интерфейса. Последняя реализованная функциональность — зачисление и групповые выплаты; для её дальнейшего развития подготовлены спецификация и подробный план.

## Направления работы

### Проверка Д89

Состояние: исправлено и проверено. `mdlZP12Validation` раньше обращался к листу «Штат» по порядковому номеру листа; после добавления новых служебных листов это давало ложное отсутствие всех сотрудников. Теперь поиск выполняется строго по имени «Штат», поэтому перестановка или добавление листов не влияет на проверку. `Test-ZP12Validation.ps1` также исправлен: он импортирует модули только во временную копию книги и не сохраняет основную `CreateOrder.xlsm`. 15.07.2026 двухпроходная регрессия пройдена (`NEW=7`, затем `OPEN=4`, `RESOLVED=3`, `RESULT: PASS`), а модуль импортирован в рабочую книгу с резервной копией `CreateOrderBackups\zp12-staff-sheet-name-20260715-202041\CreateOrder.before-zp12-staff-sheet-name.xlsm`.

Следующий шаг: обычная эксплуатационная проверка из кнопки «Проверить Д89» на пользовательском шаблоне, когда такой файл будет выбран оператором. Автоматические проверки не оставляют изменений в основной книге.

### Операторский интерфейс мастера зачисления

Последнее изменение (15.07.2026): проведена визуальная приёмка в открытой пользователем книге `CreateOrder.xlsm`, без сохранения данных и без формирования Word-файлов. Из ленты «СВО Макросы» открыта единственная операторская команда «Открыть кадровые действия», затем проверены меню и формы «Зачисление», «Кадровое перемещение» и «Исключение из списков». Мастер зачисления открывается чистой карточкой без предыдущей записи, с ожидаемыми вкладками и справочными полями. В перемещении и исключении поля не заполнены, а «Сформировать приказ» отключена до сохранения. Предыдущие автоматические доказательства сохранены: `tools/Test-EnrollmentTariffReferenceSafe.ps1` и полный `Test-PaymentsEnrollmentAcceptance.ps1` завершились успешно (`ACCEPTANCE_SMOKE_OK`).

Матрица соответствия замечаниям и доказательствам: `.spec/tasks/enrollment-operator-ui/acceptance-matrix.md`. Базовая визуальная приёмка безопасной навигации завершена. Остаются только точечная проверка раскрытия отдельных списков/разметки выплат и согласование тестовых данных для отдельного сквозного сценария с сохранением и тремя Word-приказами; неподтверждённые нормативные проценты намеренно не заполняются.

Уточнение от пользователя (14.07.2026): стандартные тарифные разряды теперь имеют краткую подпись `1 т.р.`, а стандартные звания — краткую подпись в пользовательской форме. В справочнике для ранга хранится полный код, поэтому в сохранённых данных и Word-документах звание остаётся полным. Исправлен поиск оклада по отображаемому тарифному разряду: суффикс больше не удваивается. Пройдены `tools/Test-EnrollmentTariffReferenceSafe.ps1` и полный `Test-PaymentsEnrollmentAcceptance.ps1` (`ACCEPTANCE_SMOKE_OK`).

Состояние: в работе; спецификация и план — `.spec/tasks/enrollment-operator-ui/`. Обычная команда «Зачисление» всегда начинает чистую карточку, а сохранённую карточку можно открыть только отдельной командой; поле «Признак контракта» убрано из формы как дубликат категории службы, а «Основание контракта» переименовано в «Основание службы / контракта». Звание, категория службы, ВУС, должность, подразделение, тарифный разряд, классность, ФИЗО и секретность переведены на комбинируемые поля с вводом и выбором. `EnrollmentReferenceData` хранит только расчётные величины и не получает должности, ВУС или части из `Штат`; ВУС, должность и раздел вводятся в карточке либо подставляются при выборе сотрудника. Подтверждённый «2 уровень» ФИЗО задаёт 15% и не позволяет вручную изменить процент. Лист справочника получил фиксированные ширины колонок, пригодные для ручной работы. Ежемесячные выплаты визуально разделены на блоки приказов МО № 727 и № 430дсп; разовые выплаты, персональные данные и реквизиты разделены, а дополнительные выплаты получили список часто используемых видов с возможностью ввода своего. Новая карточка сохраняет пустыми данные военнослужащего и документы, но до показа формы получает стандартные флажки и параметры ежемесячных выплат. Паспортный и банковский блоки оператор включает только при необходимости; тогда они валидируются полностью. Вкладка оснований содержит пояснение; предпросмотр Word переименован в понятные блоки статуса, исправлений, выплат и текстов двух пунктов приказа. Полный приёмочный тест завершился `ACCEPTANCE_SMOKE_OK`; после финальной косметической правки выполнена отдельная компиляция VBA.

Важное техническое решение: строка `Attribute VB_Name` должна оставаться только в экспортированном `.bas`, но при записи кода через `CodeModule.AddFromString` её нужно удалить. 14.07.2026 книга была восстановлена из резервной копии после ошибочного импорта этой строки, затем модуль импортирован безопасно и скомпилирован. Следующий шаг — перегруппировать визуальные блоки выплат, реквизитов и предварительного просмотра, затем провести ручную проверку интерфейса.

### Единый вход в кадровые действия

Состояние: базовая визуальная приёмка завершена; спецификация и план: `.spec/tasks/personnel-unified-wizard/`. На ленте одна кнопка «Открыть кадровые действия»; она открывает пользовательскую форму выбора «Зачисление», «Перемещение», «Исключение» или «История сотрудника». Оператору не нужно открывать окно Excel «Макросы» (`Alt+F8`) либо технический лист. Зачисление ведёт в существующий мастер; его сохранение создаёт ровно одну внутреннюю карточку и событие `ENROLLMENT`, а Word-экспорт сразу регистрирует файл как `ENROLLMENT_ORDER` и переводит событие в `EXPORTED`. Повторное сохранение не создаёт дубль. Перемещение и исключение открывают `frmPersonnelActionWizard`, а не технический лист. Внутри мастера выполняются загрузка состояния, сохранение и Word-экспорт, поэтому нельзя случайно запустить их вне контекста выбранного действия. Исправлена и покрыта тестом инициализация формы именно в режиме выбора действий. Исключение также переводит `Employees.IsActive` в `NO`, поэтому карточка истории корректно отражает статус исключённого сотрудника. 15.07.2026 в реальной открытой книге подтверждено: меню и все три формы открываются, зачисление начинается с пустой карточки, а экспорт перемещения/исключения недоступен без сохранённого события. Приёмка не записывала тестового военнослужащего и не создавала документы. Следующий отдельный сценарий — согласовать тестовые данные и выполнить сохранение `зачисление → перемещение → исключение` с проверкой трёх Word-приказов.

Подробная передача состояния, известных ошибок, проверок и следующего сценария: `docs/SESSION_HANDOFF_2026-07-14.md`.

| Направление | Состояние | Сделано | Следующий шаг |
|---|---|---|---|
| Зачисление и групповые выплаты | Реализована базовая версия | Добавлен поток зачисления и групповой обработки выплат (commit `e69e927`); кадровое зачисление также сохраняется в событийном журнале. | Согласовать финальные нормативные правила выплат и их связь с групповыми пакетами. |
| Кадровые события и движок надбавок | Базовый слой реализован | В книгу добавлены служебные реестры сотрудников, состояний, событий, снимков, назначений выплат, документов и НПА; лист `PersonnelEventInput` теперь создаёт назначения по введённым условиям. Для `MOBILIZED` действует отдельное назначение 158 000 ₽ по Указу № 788. | Дополнить формы и финальные Word-шаблоны. |
| Безопасный сброс кадровой формы | Реализован | Добавлен `ResetPersonnelEventInput`: очищает только значения формы, включая условия выплат и ID прошлых событий, и возвращает тип `TRANSFER`; реестры не изменяются. | Использовать перед вводом независимого нового сценария. |
| Сохранность условий надбавок | Реализована | `EmployeeCurrentState` хранит ФИЗО, спорт, медаль, дату и реквизиты приказа о награждении, водительское и контрактное основания. Пустое значение при новом событии не стирает ранее сохранённый факт; форма корректировки его подставляет. | Для старых записей без этих данных заполнить условия явно, без догадок. |
| Зачисление в кадровый журнал | Реализовано | `PersonnelEventInput` принимает `ENROLLMENT`; мастер зачисления также автоматически создаёт внутреннюю карточку, снимки, событие и текущее состояние. Его экспорт сразу регистрирует `ENROLLMENT_ORDER` по полученному `EventID`. | Согласовать финальный шаблон. |
| Корректировка кадрового события | Реализована | В `PersonnelEventInput` добавлено поле `corrects_event_id`. Новое событие сохраняется с собственной историей, а исходное получает `CORRECTED`; исходные снимки не меняются. Из карточки истории доступна подготовка формы по явно выбранному `EventID`. | Провести ручную UX-проверку после отдельного запроса. |
| Карточка истории сотрудника | Реализована | В `CreateOrder.xlsm` добавлен `mdlPersonnelHistory` и лист `PersonnelHistory`: поиск по `EmployeeID`, личному номеру или точному ФИО, вывод событий, выплат, документов и аудита синхронизаций со «Штат» из сохранённых реестров, повторный экспорт и подготовка корректировки по явно выбранному `EventID`. | При необходимости добавить команды на ленту после согласования её состава. |
| Связывание со Штат | Реализовано | Добавлены `mdlStaffLinking` и лист `StaffLinkReview`: кандидат создаётся только по точному личному номеру, а статус `LINKED` назначается только после явного действия `CONFIRM` и повторной проверки. Действие `SYNC` обновляет звание, должность и воинскую часть текущего состояния с аудитом. | Провести ручную UX-проверку после отдельного запроса. |
| Документация и преемственность | Настроено | Добавлены этот статус-файл и обязательные правила обновления документации в `AGENTS.md`. | Обновлять после каждой содержательной задачи. |
| Приказы о перемещении и исключении | Базовый Word-экспорт реализован | Лист `PersonnelEventInput` сохраняет `TRANSFER`/`EXCLUSION`; `mdlPersonnelEventOrderExport` формирует Word-проект и регистрирует файл. Исключение прекращает назначения и ставит `Employees.IsActive = NO`. | Согласовать финальные шаблоны и дополнить вывод юридически подтверждённых выплат. |
| Расширенные реквизиты перемещения и исключения | Реализованы | В событии хранятся даты сдачи, принятия и вступления, место убытия, статусы материальной помощи и двух видов отпуска. Для дат введена проверка хронологии; непустые данные выводятся в Word-проект. | Уточнить перечень выдаваемых документов и особые случаи исключения. |
| Статусы кадровых документов | Реализовано | Успешный экспорт Word создаёт запись в `DocumentRegistry` и переводит связанное событие в `EXPORTED`; отдельный Word-поток зачисления также связывается по явному EventID. | Подтвердить финальные шаблоны и юридические формулировки. |
| Связь Word-приказа зачисления с событием | Реализована | `mdlEnrollmentEventLink` регистрирует `ENROLLMENT_ORDER` и переводит событие в `EXPORTED`. В обычном мастере это происходит автоматически; ручная команда сохранена только для ранее созданных приказов. | При необходимости применить ручную команду к старым приказам. |
| Реестр нормативных актов | Реализован ввод | Добавлены `mdlLegalActs` и лист `LegalActInput`: новые записи с видом, номером, датой, редакцией и периодом действия сохраняются в `LegalActs`; прежние записи не перезаписываются. | Внести подтверждённые реквизиты НПА для 158 000 ₽, когда они будут предоставлены. |
| Каталог правил выплат | Реализован ввод черновиков | Добавлены `PaymentRules`, `PaymentRuleInput` и `mdlPaymentRuleCatalog`. Новая строка сохраняется как `DRAFT` и не включается в расчёт без отдельного подтверждения и ограниченного движка операторов. | После получения НПА утвердить правила и подключить подтверждённый набор к расчётному движку. |
| Ограничения выплат | Частично реализовано | Добавлена `PaymentCaps`; подтверждённая группа `SPECIAL_ACHIEVEMENTS_P2` хранит 100%, и расчёт пункта 2 читает предел из таблицы. | Добавлять другие группы только после подтверждения их НПА. |
| Подтверждённые основания мобилизованного | Реализованы | Для `MOBILIZED` автоматически назначается отдельная ежемесячная социальная выплата 158 000 ₽ по Указу № 788; порядок выплаты связан с приказом МО № 780 и не участвует в процентных лимитах. Неподтверждённый ФИЗО `SECOND` не назначается автоматически. | После утверждения иных оснований ФИЗО расширить правило и приёмку. |
| Классификатор должностей | Реализован ввод черновиков | Добавлены `PositionClassification`, `PositionClassificationInput` и `mdlPositionClassification` для ручной группировки `DRIVER`/`COMMAND` и иных признаков. | Подтвердить критерии и подключить их к конкретным правилам выплат. |
| Открытие книги Excel | Исправлено | Удалены дубли служебного имени `_FilterDatabase`, вызывавшие диалог «Конфликт имен» при открытии; создание дублирующих фильтров отключено. | При повторении использовать `tools/Repair-FilterDatabaseNames.ps1` только после создания резервной копии. |
| Безопасность и регрессия | Подтверждены | `Test-FIODeclension.ps1`, как и основной тест зачисления, не завершает процессы Excel/Word. При закрытом Office 14.07.2026 пройдены полный тест зачисления (`ACCEPTANCE_SMOKE_OK`) и тест склонения (`RESULT: PASS`); после завершения временных процессов Excel/Word не осталось. | Ручной E2E на согласованных тестовых данных. |

## Зафиксированные решения

### Мастер зачисления: повторная инициализация формы (14.07.2026)

- В исходнике `frmEnrollmentWizard` устранён повторный вызов `EnsureEnrollmentInfrastructure` из `UserForm_Initialize`: обычная команда открытия уже подготавливает инфраструктуру до `Show`, а повтор дополнительно обращался к шаблону Word и мог задерживать появление формы.
- Подтверждено визуально в открытой книге: модуль `mdlEnrollmentWorkflow` начинается с `Option Explicit`; служебная строка `Attribute VB_Name` в текущем кодовом модуле отсутствует. Это устраняет показанную ранее синтаксическую ошибку импорта.
- Форма импортирована в `CreateOrder.xlsm`; `Test-PaymentsEnrollmentAcceptance.ps1` завершился `ACCEPTANCE_SMOKE_OK`. Ручная проверка подтвердила: мастер открывается без VBE, за 216 мс и с пустой новой карточкой; комбобоксы, вкладки и поля окладов отображаются корректно.

- Сотрудника можно создать вручную; отсутствие на листе `Штат` не блокирует кадровое событие.
- `Штат` не является единственным хранилищем: нужна внутренняя карточка, текущее состояние и неизменяемая история событий.
- Выплаты рассчитываются по настраиваемым правилам. Неподтверждённые проценты, НПА и ограничения нельзя зашивать в VBA.
- Юридически разные основания выплат хранятся и выводятся раздельно.
- Каждое кадровое событие должно сохранять снимки состояния «до» и «после», а Word-документы — регистрироваться с метаданными.
- Для категории `MOBILIZED`: все применимые рассматриваемые надбавки, кроме ФИЗО, и отдельная фиксированная выплата 158 000 ₽ сверх остальных выплат; эта сумма регулируется отдельным НПА, а не приказом №430дсп.
- История кадровых событий реализована отдельными служебными листами, не зависящими от последующего изменения листа `Штат`: `Employees`, `EmployeeCurrentState`, `PersonnelEvents`, `PersonnelStateSnapshots`, `PaymentAssignments`, `DocumentRegistry`, `LegalActs`.
- Лист `PersonnelEventInput` поддерживает ввод `TRANSFER` и `EXCLUSION`: ввести `EmployeeID`, загрузить состояние, изменить поля `new_*`, указать даты и основание, затем запустить `SavePersonnelEventInput` через список макросов Excel.
- Лист `PersonnelEventInput` также поддерживает `ENROLLMENT`: оставить `Employee ID` пустым для автоматического внутреннего номера, заполнить `new_fio`, `new_personal_number`, состояние, даты и основание, затем запустить `SavePersonnelEventInput`.
- Перед вводом независимого нового сценария запустите `ResetPersonnelEventInput`: он очищает форму и исключает случайный перенос `corrects_event_id`, дат или условий выплат из прошлой операции.
- Условия ФИЗО, спортивного статуса, медали, даты и реквизитов приказа о награждении, водительского и контрактного оснований сохраняются в текущем состоянии. При пустом поле новой формы сохраняется предыдущее подтверждённое значение; для старых карточек без данных условие заполняется оператором явно.
- Подтверждены и реализованы: ФИЗО уровня `SECOND` — 15%; все четыре вида медалей из переданной выписки — 10%, 20% или 30% на один год со дня приказа о награждении. Без даты приказа медальное назначение помечается как требующее решения и в реестр активных выплат не попадает.
- Для корректировки заполните `corrects_event_id` идентификатором ранее сохранённого события и сохраните новое событие нужного типа. Система не перезаписывает исходную запись, а помечает её `CORRECTED`.
- Вместо ручного копирования ID откройте `PersonnelHistory`, выделите нужный `EventID` и запустите `PreparePersonnelHistoryCorrection`. Команда только подготавливает новую форму и не изменяет журнал до сохранения.
- Лист `PersonnelHistory` является режимом просмотра: он строится из кадровых реестров и не изменяет сохранённые снимки или назначения. Для повторного Word-экспорта нужно явно выделить `EventID` и запустить `ExportPersonnelHistoryEvent`.
- В `PersonnelHistory` отдельным разделом отображается аудит `StaffStateSyncLog` открытого сотрудника. Он не является кадровым событием и не доступен для Word-экспорта.
- Лист `StaffLinkReview` никогда не связывает карточку автоматически. Запустить `BuildStaffLinkCandidates`, проверить строку `CANDIDATE`, поставить `CONFIRM` в столбце `Action` и запустить `ConfirmStaffLinkSelections`. Для отказа используется `REJECT` и `RejectStaffLinkSelections`.
- После подтверждения связи можно поставить `SYNC` в столбце `Action` и запустить `SyncConfirmedStaffState`. Повторно проверяются личный номер и ссылка на строку `Штат`; обновляются только звание, должность и воинская часть текущего состояния. Кадровое событие не создаётся, а результат записывается в `StaffStateSyncLog`.
- После `ExportPersonnelEventOrder` в `PersonnelEvents` у соответствующего события будет статус `EXPORTED`; путь к созданному файлу хранится в `DocumentRegistry`.
- Для ввода НПА запустите `OpenLegalActInput`, заполните реквизиты и выполните `SaveLegalActInput`. Новая редакция должна иметь новый `ActID`.
- После существующего экспорта приказа зачисления запустите `RegisterEnrollmentOrderForEventPrompt`, укажите сохранённый `ENROLLMENT` EventID и путь к Word-файлу. Автоматическое сопоставление по ФИО и датам не выполняется.
- Для подготовки нового правила запустите `OpenPaymentRuleInput` и сохраните его через `SavePaymentRuleInput`. Оно получит статус `DRAFT` и не начнёт влиять на начисления автоматически.
- На листе `PaymentCaps` находится подтверждённый предел 100% для `SPECIAL_ACHIEVEMENTS_P2`. Изменять его следует только после подтверждения новой нормы; остальные группы не заполняются предположениями.
- Для подготовки классификации должности запустите `OpenPositionClassificationInput` и сохраните запись. Она имеет справочный статус `DRAFT` и не назначает выплаты автоматически.

## Открытые вопросы

1. Утвердить нормативные основания, сроки и точные размеры для оставшихся неподтверждённых выплат.
2. Утвердить полный перечень применимых в части оснований пунктов 2, 3 и 3.4; лимит 100% уже зафиксирован только для оснований пункта 2, а тарифная надбавка пункта 3 и 60%-ная надбавка за контракт пункта 3.4 начисляются сверх него.
3. Утвердить формулировки и шаблоны Word-документов для зачисления, перемещения и исключения.
4. Для иных неподтверждённых выплат по-прежнему требуются точные НПА, периоды и подтверждающие документы. Выплата 158 000 ₽ исключена из этого вопроса: её основание подтверждено Указом № 788, порядок — приказом МО № 780.

## Аудит готовности на 13.07.2026

- Подтверждённо работает: события `ENROLLMENT`/`TRANSFER`/`EXCLUSION`, снимки, корректировки, история, ручное связывание со `Штат`, Word-регистрация, отдельная связь Word-приказа зачисления, реестры НПА/правил и конфигурируемый предел пункта 2.
- Не объявлено готовым к нормативному применению: новые черновики `PaymentRules`, иные неподтверждённые основания и дополнительные группы ограничений.
- Причина: отсутствуют подтверждённые реквизиты и/или утверждённые формулировки из открытых вопросов выше; приложение намеренно не активирует эти данные автоматически.
- Дополнительно проверено: фиксированная выплата 158 000 ₽ для `MOBILIZED` создаётся как отдельное активное назначение с предустановленным `ActID` Указа № 788.
- Проверка 13.07.2026: `powershell -ExecutionPolicy Bypass -File .\Test-PersonnelEvents.ps1` успешно завершён после импорта модуля в книгу; копия до импорта — `CreateOrderBackups\CreateOrder_before_allowance_guard_20260713_230149.xlsm`.
- Проверка 13.07.2026: подготовка корректировки из `PersonnelHistory` переносит выбранный `EventID` в форму и не создаёт запись до явного сохранения; копия до импорта — `CreateOrderBackups\CreateOrder_before_history_correction_20260713_230704.xlsm`.
- Проверка 13.07.2026: условия ФИЗО, спорта, медали, водителя и контракта сохраняются после перемещения и автоматически подставляются при подготовке корректировки; изолированная приёмка пройдена.
- Проверка 13.07.2026: `ResetPersonnelEventInput` очищает все значения формы и не создаёт строку в `PersonnelEvents`; изолированная приёмка пройдена.
- Проверка 13.07.2026: действие `SYNC` для подтверждённой строки `LINKED` обновляет только звание, должность и воинскую часть, сохраняет аудит и не меняет события или назначения; изолированная приёмка пройдена.
- Проверка 13.07.2026: аудит `StaffStateSyncLog` по открытому `EmployeeID` отображается в `PersonnelHistory`; изолированная приёмка подтверждает, что он не смешивается с кадровыми событиями и выплатами.
- Проверка 13.07.2026: для `MOBILIZED` 158 000 ₽ получают статус `ACTIVE`, `ActID` Указа № 788 и отдельную строку Word; Word-проект выводит самостоятельные вводные абзацы № 430дсп и № 788, а предел пункта 2 остаётся отдельным.
- Проверка 13.07.2026: `powershell -ExecutionPolicy Bypass -File .\Test-PersonnelEvents.ps1` успешно завершён после импорта в рабочую книгу; резервная копия до импорта — `CreateOrderBackups\CreateOrder_before_legal_word_groups_20260713_234646.xlsm`. Визуально проверен Word-проект: шапка центрирована, текст приказа выровнен по левому краю, длинные нормативные абзацы помещаются без наложений.
- Проверка 13.07.2026: расширенные реквизиты кадрового события добавлены в `PersonnelEvents`; рабочая книга содержит новые заголовки и Word-модуль с выводом дат/данных исключения. Регрессионная приёмка пройдена до импорта; резервная копия — `CreateOrderBackups\CreateOrder_before_event_details_20260713_235727.xlsm`.
- Документация 13.07.2026: план по образцам перемещения и исключения синхронизирован с реализацией. Базовый индивидуальный Word-экспорт уже есть; утверждённые шаблоны, поля отпусков/материальной помощи/убытия и групповые приказы пока не реализованы.
- Реализация и проверка 14.07.2026: `SECOND` создаёт активное назначение ФИЗО 15%. Для всех четырёх медалей из выписки созданы срочные назначения 10/20/30%: дата начала — дата приказа о награждении, окончание — день перед первой годовщиной; в `EmployeeCurrentState` и форме сохранены дата и реквизиты приказа. Обновлённые модули импортированы в `CreateOrder.xlsm`; изолированная приёмка пройдена. Резервная копия: `CreateOrderBackups\CreateOrder_before_fizo_medals_20260714_000611.xlsm`.

## Документы и ориентиры

- Бизнес-правила: `.spec/tasks/enrollment-allowance-rules/spec.md`
- Полный план реализации: `.spec/tasks/enrollment-allowance-rules/plan.md`
- Расширенная спецификация зачисления и выплат: `docs/FeatureSpec_PaymentsAndEnrollment.md`
- Разбор выписки приказа №430дсп: `.spec/tasks/enrollment-allowance-rules/source-analysis-430.md`
- План по приказам о перемещении и исключении: `.spec/tasks/enrollment-allowance-rules/order-forms-move-exclusion.md`
- Реализация реестров и правил: `CreateOrder.xlsm.modules/mdlPersonnelEvents.bas`, `CreateOrder.xlsm.modules/mdlPersonnelAllowanceRules.bas`
- Word-экспорт кадровых приказов: `CreateOrder.xlsm.modules/mdlPersonnelEventOrderExport.bas`
- Изолированный приёмочный тест: `Test-PersonnelEvents.ps1` (перемещение, исключение, прекращение выплат, `MOBILIZED`, потолок пункта 2 и вывод всех исходных оснований в Word)
- Технический контекст и журнал изменений: `.spec/PROJECT_CONTEXT.md`

## Как продолжить работу

1. Прочитать этот файл и `.spec/PROJECT_CONTEXT.md`.
2. Открыть спецификацию и план кадровых событий.
3. Ответить на открытые вопросы либо явно оставить их конфигурируемыми.
4. Перед изменением кода создать отдельную ветку и обновить спецификацию.
5. После реализации выполнить адресную проверку, обновить этот файл, контекст и пользовательское руководство при необходимости.

## Ручная проверка интерфейса (13.07.2026)

Начата сквозная проверка: зачисление тестового военнослужащего, перемещение, исключение и выпуск приказов. Перед запуском создана резервная копия `CreateOrder_before_manual_ui_e2e_20260713_224251.xlsm`.

При проверке открыта рабочая книга и подтверждено наличие листа `PersonnelEventInput`. Дальнейшее управление Excel через Computer Use потеряло активное окно до сохранения формы. Ввод остановлен: тестовый человек, кадровые события и Word-приказы не созданы. Автоматическая приёмка проходит, но ручной сценарий пока не считается пройденным. Его нужно повторить с чистого состояния при устойчивом доступе к окну Excel.
