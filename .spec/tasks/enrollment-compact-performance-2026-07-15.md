# Компактный мастер зачисления и производительность

## Проблема

Текущий мастер зачисления показывает длинные вкладки с вертикальной прокруткой, а прокрутка колесом в UserForm работает ненадёжно. Открытие формы и экспорт Word занимают заметное время. Оклады по званию и тарифному разряду остаются пустыми, если в справочнике нет суммы.

## Требования

- Все операторские вкладки помещаются в одном окне без вертикальной прокрутки; поля распределены по смысловым блокам.
- Вкладка документов: отдельно реквизиты приказа, документы прибытия, даты и основания.
- Вкладка разовых выплат: отдельно разовые выплаты, паспортные данные и банковские реквизиты.
- Вкладки ежемесячных, оснований, иных выплат и проверки также не используют вертикальную прокрутку. Для «иных выплат» показывается ограниченное число компактных строк, а не длинная простыня.
- Инфраструктура зачисления подготавливается один раз за сеанс Excel. Экспорт не должен повторно выполнять подготовку/синхронизацию перед каждым шагом.
- Поле оклада по званию и по должности заполняется при выборе из `EnrollmentReferenceData`; если сумма для выбранной строки отсутствует, форма прямо показывает, что нужно заполнить столбец `Amount` соответствующей строки, а не создаёт пустое неясное поле.
- Справочник должен содержать проверяемые значения окладов по состоянию на конкретную дату, не перезаписывать ручную корректировку оператора и не создавать дубли рангов при подготовке.
- Справочник `EnrollmentReferenceData` — только регистр расчётных значений: оклады званий и разрядов, проценты/размеры выплат, а также реквизиты банка. Он не импортирует из «Штата» должности, ВУС, разделы или воинские части.
- Поля персональных и банковских реквизитов включаются оператором отдельными флажками. Пока флажок выключен, пустые поля не блокируют проверку и Word-экспорт; при включении полные проверки этих блоков обязательны.
- Сведения о прибытии и документы должны иметь отдельные флажки: «предписание / источник прибытия» и «рапорт / регистрация». Выключенный блок не обязателен, не валидируется и не выводится в Word даже при наличии ранее введённого текста.
- Вкладка документов должна показывать реквизиты приказа, прибытие и рапорт как отдельные визуальные блоки; ни один элемент блока не должен обрезаться границей фрейма. Вкладка оснований должна прямо объяснять, что текст основания используется в Word только для включённой выплаты, и все поля, включая премию и особые достижения, должны быть полностью видимы.
- Вкладка ежемесячных выплат должна размещать элементы по сетке без обрезания даты и номера приказа. Надбавка 1–4 тарифных разрядов относится к блоку №430дсп, а надбавка за работу со сведениями, составляющими государственную тайну — к блоку №727.
- Надбавка 1–4 тарифных разрядов назначается исключительно автоматически: при выборе на первой вкладке разряда 1–4 она включается, при выборе другого разряда — выключается. Оператор видит результат и процент, но не меняет его флажком. Льготная выслуга находится отдельным верхним блоком, включена по умолчанию и формирует свой абзац Word. Вкладка индивидуальных оснований не показывает и не хранит основания для автоматических выплат: льготной выслуги, надбавки по должности, особых условий и 1–4 тарифа.
- После Word-экспорта форма показывает полный путь в отдельном поле результата. Файл приказа зачисления сохраняется в папке с книгой по шаблону имени из настройки `enrollment.filename_template`.

## Реализация

- `frmEnrollmentWizard`: все семь вкладок рассчитаны на одну высоту окна, вертикальная прокрутка отключена; документы, даты, основания и иные выплаты разбиты на компактные блоки.
- `mdlEnrollmentWorkflow`: подготовка инфраструктуры и справочника выполняется один раз за сеанс; перед первой синхронизацией удаляются дубли по типу и коду.
- При выборе ранга или тарифного разряда поле `Amount` подставляется из справочника. Если сумма пуста, выводится объяснение, какой столбец `Amount` требуется заполнить.
- После Word-экспорта путь сохраняется в поле результата вкладки проверки; статус ожидания выводится только перед экспортом.
- Ежемесячные выплаты разделены на независимые фреймы приказов №727 и №430дсп; иные выплаты хранят реквизиты другого приказа и выводят их в Word.
- Мастер открывается немодально, поэтому оператор может править `EnrollmentReferenceData` без потери введённой карточки.
- `SeedCurrentContractSalaryReferenceData` заполняет только пустые `Amount`: 50 тарифных разрядов и общевойсковые звания от рядового до полковника по состоянию на 01.10.2025. Основание — постановление Правительства РФ №464 в редакции №1390 (коэффициент 1,076) и приложения №2/№3 к приказу МО РФ №727. После синхронизации повторные строки рангов удаляются.
- `EnrollmentReferenceData` очищает и больше не создаёт строки `POSITION`, `VUS`, `SECTION` и `MILITARY_UNIT`; код синхронизации со «Штатом» удалён. Убрано дублирующее поле воинской части. Флажки персонального и банковского блока управляют обязательностью реквизитов.
- Вкладка документов переразмечена фреймами; добавлены флажки прибытия и рапорта. Выключенный блок очищается из записи и исключается из Word. Вкладка оснований сгруппирована по №727/№430дсп/разовым выплатам. Надбавка 1–4 тарифных разрядов перенесена в №430дсп, секретность — в №727; сетка ежемесячных выплат выровнена, реквизиты наградного приказа не обрезаются. Высота фреймов документов и оснований увеличена с запасом: полностью видны «Предписание / основание», «Рапорт / регистрация», кадровые даты, премия и особые достижения. Для каждой включённой типовой выплаты её индивидуальное основание дописывается в соответствующий Word-абзац; выключенная выплата его не выводит.
- Льготная выслуга вынесена из блока №727 в отдельную верхнюю строку, остаётся включённой по умолчанию и выводится в Word отдельным абзацем. `std_tariff_enabled` больше не является ручным решением: нормализация и форма всегда вычисляют его из тарифного разряда 1–4. Из вкладки оснований и из данных карточки очищаются основания льготной выслуги, надбавки по должности, особых условий и 1–4 тарифа; для них не требуется индивидуальный документ и такой текст не выводится в Word.

## Проверка

1. Пройден `tools/Test-EnrollmentCompactUiSafe.ps1`: повторная инициализация инфраструктуры, очистка раздутого справочника, семь вкладок и отсутствие ScrollBars.
2. Пройден `tools/Test-EnrollmentTariffReferenceSafe.ps1`: `Amount` находится и по краткому отображению, и по коду тарифа/звания.
3. Повторно пройден `tools/Test-EnrollmentCompactUiSafe.ps1`: в справочнике нет штатных типов, а на вкладке реквизитов есть оба флажка. Загрузка формы — 0,502 секунды.
4. Повторно пройден `tools/Test-EnrollmentTariffReferenceSafe.ps1`: пустые выключенные блоки реквизитов не блокируют Word; включённый блок данных требует заполнения.
5. Исходники сразу импортированы в рабочую `CreateOrder.xlsm`; резервная копия: `CreateOrderBackups\enrollment-compact-ui-installed-20260716-000859\CreateOrder.before-enrollment-compact-ui.xlsm`.
6. Следующая проверка — ручная визуальная приёмка без Computer Use.
7. Пройдены дополнительные регрессии: отключённые документы не содержатся в Word-предпросмотре, а направления `std_tariff → Section1MonthlyPersonal (№430дсп)` и `secrecy → Section1MonthlyStandard (№727)` подтверждены. Импорт: `CreateOrderBackups\enrollment-compact-ui-installed-20260716-003558\CreateOrder.before-enrollment-compact-ui.xlsm`.
8. Пройдены повторные `Test-EnrollmentCompactUiSafe.ps1` (0,475 секунды) и `Test-EnrollmentTariffReferenceSafe.ps1`; второй тест подтверждает появление индивидуального основания включённой ФИЗО в Word-предпросмотре. Исходники импортированы в рабочую книгу; резервная копия: `CreateOrderBackups\enrollment-compact-ui-installed-20260716-004650\CreateOrder.before-enrollment-compact-ui.xlsm`.
